import React from 'react';

function Header() {
    const message = 'Hello World!'
    return (
        <div className="App">
            <h1>{message}</h1>
        </div>
    );
}

export default Header;